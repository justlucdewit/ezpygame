from ezpg import *

class app(sketch):
    def setup():
        createCanvas(600, 600)
        background(100, 100, 100)
        fill(255, 0, 0)
        point(10, 10)
    def draw():
        pass

start(app)
